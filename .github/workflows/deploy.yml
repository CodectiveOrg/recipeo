name: Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: BijanProgrammerWebsites/hub/.github/workflows/deploy-vite.yml@master
    with:
      host: recipeo.ir
      base-href: /
    secrets:
      ENV_FILE: ${{ secrets.ENV_FILE }}
